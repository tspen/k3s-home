---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: radarr
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: radarr-pv
  storageClassName: nfs-csi